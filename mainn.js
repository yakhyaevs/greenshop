console.log("started...");
new Promise((reject, resolve) => {
  reject("Welcome to");
}).then((value) => console.log(value));
console.log("ended...");

class Newreact {
  constructor(car,color)
  function mixedStructure(car,color){
    return `${car} and ${color}}};
